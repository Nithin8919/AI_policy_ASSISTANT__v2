# Core PDF Processing
pdfplumber>=0.9.0
PyPDF2>=3.0.0
pdf2image>=1.16.0

# OCR (optional but recommended)
pytesseract>=0.3.10
Pillow>=10.0.0
fastapi>=0.104.1
uvicorn[standard]==0.24.0

# LLM
google-generativeai>=0.3.0

# Data handling
jsonlines>=3.1.0

# Embeddings (when ready to implement)
sentence-transformers>=2.2.0
qdrant-client>=1.7.0
torch>=2.0.0
numpy>=1.24.0

# Logging
colorlog>=6.7.0

# Environment variables
python-dotenv>=1.0.0

# Testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0

# Vertex AI (GCP Native)
google-cloud-aiplatform>=1.38.0

# Google Cloud Storage (for PDF viewer backend)
google-cloud-storage>=2.10.0

# Content extraction (same as before)
trafilatura>=1.6.0
requests>=2.31.0

# Already in your requirements (keep them)
# fastapi
# uvicorn
# python-dotenv
# qdrant-client
# sentence-transformers

# Google Cloud Storage for signed URLs and PDF access
google-cloud-storage>=2.10.0

# PDF text extraction (already in main requirements.txt)
# pdfplumber>=0.9.0

# Testing dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0  # For testing FastAPI endpoints
